include "includes-l1.hpp"

class Pose{

  frame storeFrame;
  float nomTime;       //nominal time to snap to
  float i;            // position in the index;
  public:
    Pose(frame f, int index){
      storeFrame = frame;
    }
}
